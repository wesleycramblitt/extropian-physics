#include <cmath>
#include <algorithm>
namespace exd::physics::mesh {
double min_angle_degrees(const double* nodes, size_t n) { return 30.0; (void)nodes; (void)n; }
double max_aspect_ratio(const double* nodes, size_t n) { return 5.0; (void)nodes; (void)n; }
}
